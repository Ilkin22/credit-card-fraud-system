"""Initialize the src package.

This file marks the `src` directory as a Python package so that
modules such as `src.train_model` and `src.utils` can be imported
using absolute imports within the project (e.g., `from src.utils import FEATURES`).
"""
